#include <stdio.h>
#include <string.h>
int main ()
{
    int i;
    char str[1001];
    scanf("%s",str);
    for(i=1; i<1001; i++)
    {
        if(str[i-1]==' ')
        {
            if(str[i]=='i');
            else if(str[i]=='a');
        }
    }
    return 0;
}
